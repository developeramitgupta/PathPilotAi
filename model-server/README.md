# Model server

Reserved for a future standalone AI inference or orchestration service. The current production-safe AI routes remain in `frontend/src/app/api` so they share authentication, validation, rate limits, and audit controls with the web app.
